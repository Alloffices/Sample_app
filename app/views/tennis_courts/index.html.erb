<% if user_signed_in? %>

	<% @tennis_courts.each do |tennis_court| %>
	  <div class="tennis_court">
	    <%= tennis_court.name %>
	    <%= link_to "Show all bookings", tennis_court_bookings_path(tennis_court) %>
  <!-- link_to "Delete", tennis_court_path(tennis_court), data: { confirm: 'Are you sure?' }, method: :delete %>
 link_to "Edit", edit_tennis_court_path(tennis_court) %> -->
	    <%= link_to "Add booking", new_tennis_court_booking_path(tennis_court), class: "btn btn-success" %>
	  </div>
	  
	<% end %>
	<!-- this a back linkt to the All tennis courts list -->
<!-- link_to "New Space Booking", new_tennis_court_path %> -->
<% else %>

      </div>

                <ul class="nav navbar-nav navbar-right">
                    <li><%= link_to "Sign in", new_admin_session_path %></li>
                    <li><%= link_to "Sign Up", new_admin_registration_path, class: "button" %></li>
                </ul>
    </footer>

<% end %>
